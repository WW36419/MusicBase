import { ArtistCheckPipe } from './artist-check.pipe';

describe('ArtistCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
